import { TimeAmountPipe } from './time-amount.pipe';

describe('TimeAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
